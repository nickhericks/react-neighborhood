import React, { Component} from 'react';

// React Component to display Facebook API data on number of FB Likes
// Also displays FB Friends that Like it with user image
export default class FbError extends Component {
  render() {
    return (
      <div>
        Hello
      </div>
    );
  }
}
